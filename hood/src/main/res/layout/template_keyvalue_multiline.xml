<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    style="@style/HoodLibKeyValue"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:id="@+id/inner_wrapper"
        style="@style/HoodLibKeyValueInner"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/key"
            style="@style/HoodLibTextViewSingleLine.HoodLibKeyValueTextView.Key"
            android:background="@android:color/transparent"
            tools:text="version"/>

            <TextView
                android:id="@+id/value"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:fontFamily="monospace"
                android:paddingTop="12dp"
                android:maxHeight="80dp"
                android:typeface="monospace"
                tools:text="v2.3.4 this is longer and longer and so on this goes \non and on\nmultiline stuff\nmultiline stuff\nmultiline stuff\nmultiline stuff\nmultiline stuff"/>
    </LinearLayout>
</FrameLayout>